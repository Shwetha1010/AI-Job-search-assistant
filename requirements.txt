# requirements.txt
# Used by the Streamlit frontend HuggingFace Space.
# LIGHTWEIGHT — frontend only makes HTTP calls to the backend.
# No torch, no ML libraries needed here.

streamlit>=1.35.0
requests>=2.31.0
python-dotenv>=1.0.0
